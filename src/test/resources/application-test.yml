springdoc:
    api-docs:
        enabled: false
spring:
    main:
        lazy-initialization: true
        banner-mode: off
    jpa:
        show-sql: false
    data:
        jpa:
            repositories:
                bootstrap-mode: lazy
    jmx:
        enabled: false
logging.level:
    ROOT: ERROR
